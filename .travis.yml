language: python

env:
  - TEST_NETWORK=0
  - TEST_NETWORK=1

before_install:
  - sudo apt-get update
  - sudo apt-get install -y -q fakeroot fakechroot cowdancer python-dateutil

install:
  - python bootstrap.py
  - ./bin/buildout

script: ./bin/test discover

